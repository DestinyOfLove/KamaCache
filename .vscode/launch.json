{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/out/build/base/main",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake: build"
        }
    ]
}